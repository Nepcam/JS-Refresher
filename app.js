const firstName = 'Cameron';
const lastName = 'Nepe';
const age = 44;

let val;

val = firstName + lastName;

// Concatenation
val = firstName + ' ' + lastName;

// Append
val = 'John ';
val += 'Doe';

val = 'Kia ora, my name is ' + firstName + ' and I am ' + age;

// Output
console.log(val);
